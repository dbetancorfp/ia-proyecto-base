Verifica que el proyecto compila sin errores y muestra un resumen del estado.

Ejecuta `npm run build` y analiza la salida:
- Si hay errores TypeScript, identifica el archivo y línea, explica la causa y propón la corrección
- Si compila bien, muestra el tamaño de los bundles generados en `out/`
- Indica si hay warnings relevantes

No arranques la app completa, solo verifica la compilación.
